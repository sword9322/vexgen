# ============================================================
# VoxPrompt – Environment Variables
# ============================================================
# 1. Copy this file:   cp .env.example .env.local
# 2. Fill in your real values — DO NOT leave placeholders.
# 3. Restart the dev server after every change: npm run dev

# ------------------------------------------------------------
# OpenAI API Key (REQUIRED)
# ⚠️  Replace REPLACE_WITH_YOUR_REAL_KEY with your actual key.
# Get one at https://platform.openai.com/api-keys
# ------------------------------------------------------------
OPENAI_API_KEY=sk-proj-REPLACE_WITH_YOUR_REAL_KEY

# ------------------------------------------------------------
# Corporate / enterprise proxy (OPTIONAL)
# If your corporate firewall blocks direct connections to
# api.openai.com, add your proxy URL here.
# The app will automatically route all OpenAI requests through it.
# Example: HTTPS_PROXY=http://proxy.yourcompany.com:8080
# Leave commented out on a direct internet connection.
# ------------------------------------------------------------
# HTTPS_PROXY=http://proxy.yourcompany.com:8080

# ------------------------------------------------------------
# Rate limiting (optional – defaults shown)
# Max requests per minute per IP address
# ------------------------------------------------------------
TRANSCRIBE_RATE_LIMIT=10
GENERATE_RATE_LIMIT=20

# ------------------------------------------------------------
# Max audio file size in bytes (default: 25 MB = 26214400)
# Whisper API hard limit is 25 MB
# ------------------------------------------------------------
MAX_AUDIO_SIZE=26214400
